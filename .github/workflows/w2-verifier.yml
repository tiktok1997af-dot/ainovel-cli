name: W2 Browser Verifier Build

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-verifier:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build W2 verifier
        shell: pwsh
        env:
          GOWORK: "off"
        run: |
          New-Item -ItemType Directory -Force dist | Out-Null
          go build -trimpath -o dist/ainovel-w2-verify.exe ./cmd/ainovel-w2-verify
          Copy-Item scripts/w2-verify-windows.cmd dist/START_W2_VERIFY.cmd
          Get-FileHash dist/ainovel-w2-verify.exe -Algorithm SHA256 | Format-List

      - name: Upload W2 verifier
        uses: actions/upload-artifact@v4
        with:
          name: ainovel-w2-verifier-windows
          path: dist/*
          if-no-files-found: error
          retention-days: 7
