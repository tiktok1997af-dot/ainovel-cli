package version

// ProductRepository is the only release repository used by the ainovel-cli
// product updater and installation instructions. The updater transport remains
// generic, but product wiring must never fall back to the upstream API-era fork.
const ProductRepository = "tiktok1997af-dot/ainovel-cli"
